--------------------------------------------------------------------------------
-- procedure to fill the screen with one color
--------------------------------------------------------------------------------
procedure lcd_fill(byte in color) is
   -- some code to fill the screen with background color
end procedure

--------------------------------------------------------------------------------
-- draw one pixel: lcd_write_pixel(x,y,color)
--------------------------------------------------------------------------------
procedure _lcd_write_pixel(byte in x,byte in y,byte in color) is
   -- some code to put a pixel on the screen
end procedure

-- if cache is not enalbed, rename _lcd_write_pixel to lcd_write_pixel
if !defined(glcd_write_cache) then
   alias lcd_write_pixel is _lcd_write_pixel
end if

--------------------------------------------------------------------------------
-- init procedure
--------------------------------------------------------------------------------
procedure glcd_init() is

end procedure
